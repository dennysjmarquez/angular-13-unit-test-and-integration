export function incrementar(numero: number) {
  if(numero > 100){
    return 100
  }else {
    return numero + 1
  }
}

/*
export function incrementar(numero: number): any {
  if(numero > 100){
    return 100
  }else {
    //return numero + 1
  }
}
*/
